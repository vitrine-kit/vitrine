@maks417:registry=https://npm.pkg.github.com
# Приватные пакеты @maks417/* требуют GitHub PAT со scope read:packages.
# Экспортируйте GITHUB_TOKEN перед установкой (dev, CI, docker build).
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
