node_modules/
dist/
.turbo/
coverage/
*.log
*.tsbuildinfo

# Environment and local dev data
.env
.env.local
.env.*.local
.vitrine/

# OS / editors
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# AI assistants / IDE rules / local docs / internal planning — not committed by default.
# Paths are anchored to the root (leading /) so we don't accidentally match future
# shipped files like templates/base/CLAUDE.md (a client deliverable) or docs inside packages.
/CLAUDE.md
/.claude/
/.cursor/
/docs/
/IMPLEMENTATION_PLAN.md
