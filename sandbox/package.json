{
  "name": "sandbox",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Core-development playground: runs the feature registry on the contracts alone.",
  "scripts": {
    "demo": "tsx src/demo.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@vitrine-kit/contracts": "workspace:*",
    "@vitrine-kit/core": "workspace:*",
    "@vitrine-kit/payload-blueprint": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "vitest": "^2.1.8"
  }
}
