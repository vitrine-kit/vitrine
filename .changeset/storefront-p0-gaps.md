---
"@vitrine-kit/contracts": minor
"@vitrine-kit/vitrine": patch
---

Close the highest-impact simple-store gaps from live client review: treat blank `PAYLOAD_SECRET` as unset and write a filled `.env` at init; add `/order/success`; wire cart qty/remove + badge; drive storefront branding from `siteConfig.name`; return JSON checkout/cart errors; enforce stock on add/checkout.
