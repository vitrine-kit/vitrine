---
'@vitrine-kit/payload-blueprint': minor
'@vitrine-kit/vitrine': patch
---

Deepen remaining storefront ops: SMTP via `@payloadcms/email-nodemailer` in Payload config, customer register/login (`customers` collection via blueprint `addCollection`), and locale path-prefix middleware when `locales.length > 1`.
